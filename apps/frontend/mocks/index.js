const mockConnections = require('./connections.json')
const mockContent = require('./content.json')
const mockModels = require('./models.json')
const mockBots = require('./bots.json')
const mockTodo = require('./todo.json')

module.exports = {
  mockConnections,
  mockContent,
  mockModels,
  mockBots,
  mockTodo,
}
