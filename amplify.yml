version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm install -g pnpm
            - pnpm install
        build:
          commands:
            - cd apps/frontend && pnpm run build
      artifacts:
        baseDirectory: apps/frontend/dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: .
