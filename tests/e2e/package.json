{
  "name": "@baita/e2e",
  "private": true,
  "scripts": {
    "test": "TEST_ENV=local playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "verify": "TEST_ENV=local PWDEBUG=1 playwright test verify.spec.ts --project=e2e --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.50",
    "typescript": "^5.9"
  }
}
